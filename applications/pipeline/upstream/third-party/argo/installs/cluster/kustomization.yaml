apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
# Vendored from argo-workflows v3.7.3 — see ../../vendor/README.md
- ../../vendor/crds-minimal
- ../../vendor/cluster-install-wfc-rbac
- ../../base

patches:
- path: workflow-controller-clusterrolebinding-patch.json
  target:
    group: rbac.authorization.k8s.io
    kind: ClusterRoleBinding
    name: argo-binding
    version: v1
